# JobMatch AI

> 智能求职匹配助手 - 让每一次投递决策"可解释、可复盘、可优化"

JobMatch AI 是一款面向中高级技术人才的求职决策助手。通过 LLM 结构化分析简历与职位描述的匹配度，提供可解释的评估结果和可执行的行动建议。

## 核心特性

- **本地优先** - 使用本地 Ollama LLM，数据不出本机
- **可解释分析** - 每项评分都有证据支撑，不输出"黑盒分数"
- **结构化报告** - 硬性门槛检查 → 软性评分 → 差距分析 → 行动建议
- **智能降级** - LLM 不可用时自动切换规则引擎
- **配置驱动** - 业务规则和参数外置，支持用户自定义

## 快速安装

```bash
# 1. 确保已安装 Java 17+ 和 Ollama
java -version
ollama --version

# 2. 启动 Ollama 并下载模型
ollama serve &
ollama pull qwen2.5:7b

# 3. 安装 JobMatch
git clone https://github.com/justone25/job-match-ai.git && cd job-match-ai
./install.sh
```

---

## analyze 命令详解

`analyze` 是核心命令，用于分析简历与 JD 的匹配度。支持三种输入模式：

### 用法一：文件模式（推荐）

直接指定简历和 JD 文件路径，支持 **PDF、TXT、MD** 格式：

```bash
# 基本用法（支持 PDF）
jobmatch analyze -r resume.pdf -j job.txt

# 或使用 TXT/MD 文件
jobmatch analyze -r resume.txt -j job.txt

# 完整参数
jobmatch analyze \
  --resume resume.txt \
  --jd job.txt \
  --format markdown \
  --output report.md
```

**示例：**
```bash
# 使用项目自带的示例文件
jobmatch analyze -r examples/sample_resume.txt -j examples/sample_jd.txt

# 保存报告到文件
jobmatch analyze -r my_resume.txt -j target_job.txt -o match_report.md
```

### 用法二：文本模式

直接传入文本内容（适合脚本调用或短文本）：

```bash
# 使用文本参数
jobmatch analyze \
  --resume-text "张三，8年Java开发经验，精通Spring Boot、MySQL..." \
  --jd-text "招聘高级Java工程师，要求5年以上经验..."
```

### 用法三：交互模式

不带参数启动，进入交互式输入：

```bash
jobmatch analyze
```

交互流程：
```
╔══════════════════════════════════════════════════╗
║     JobMatch AI v0.1.0 - 交互模式              ║
╚══════════════════════════════════════════════════╝

请输入简历内容 (输入空行结束):
──────────────────────────────────────────────────
张三
8年Java开发经验
精通 Spring Boot、MySQL、Redis
...
<空行结束>

请输入职位描述 (输入空行结束):
──────────────────────────────────────────────────
高级Java开发工程师
要求5年以上经验...
<空行结束>

[1/3] 解析简历中...
  ✓ 提取到 12 项技能, 3 段工作经历
[2/3] 解析职位描述中...
  ✓ 提取到 8 项硬性要求, 4 项软性要求
[3/3] 匹配分析中...
  ✓ 分析完成
  ✓ 已保存 (ID: 20231213_143052)
```

### 输出格式选项

使用 `-f` 或 `--format` 指定输出格式：

```bash
# Markdown 格式（默认，适合阅读）
jobmatch analyze -r resume.txt -j job.txt -f markdown

# JSON 格式（适合程序处理）
jobmatch analyze -r resume.txt -j job.txt -f json

# 简洁格式（只显示关键信息）
jobmatch analyze -r resume.txt -j job.txt -f simple
```

### 参数速查表

| 参数 | 短写 | 说明 | 示例 |
|------|------|------|------|
| `--resume` | `-r` | 简历文件路径 (PDF/TXT/MD) | `-r resume.pdf` |
| `--jd` | `-j` | JD 文件路径 (PDF/TXT/MD) | `-j job.txt` |
| `--resume-text` | | 简历文本内容 | `--resume-text "..."` |
| `--jd-text` | | JD 文本内容 | `--jd-text "..."` |
| `--format` | `-f` | 输出格式 | `-f json` |
| `--output` | `-o` | 输出文件路径 | `-o report.md` |
| `--no-cache` | | 禁用缓存 | `--no-cache` |
| `--dry-run` | | 仅解析不匹配 | `--dry-run` |

---

## 匹配引擎架构

### 四阶段分析流程

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  HardGateChecker│ ──▶ │SoftScoreCalculator│ ──▶ │   GapAnalyzer   │ ──▶ │ ActionGenerator │
│   硬性门槛检查    │     │   软性评分计算      │     │    差距分析     │     │   行动建议生成  │
└─────────────────┘     └─────────────────┘     └─────────────────┘     └─────────────────┘
        │                       │                       │                       │
        ▼                       ▼                       ▼                       ▼
   经验/学历/技能          技能/经验/加分项           缺失技能/优势         简历优化/学习计划
   PASS/FAIL/边界          加权评分 0-100           可提升空间             具体建议
```

### 技能推断机制

系统支持三层技能匹配：

1. **直接匹配** - 简历技能与 JD 要求完全匹配
2. **规则推断** - 基于 `skill_implications.yaml` 中的 27 条规则推断
3. **LLM 兜底** - 规则无法覆盖时，调用 LLM 推断技能关联

```
例如：候选人会 Spring Boot → 可推断具备 Java 编程能力 (置信度 95%)
```

### 配置驱动设计

所有业务规则和参数均可通过 YAML 配置文件调整，支持 5 层优先级覆盖：

```
优先级 (低→高):
1. 内置默认值 (JAR 包内)
2. 用户目录 (~/.jobmatch/*.yaml)
3. 当前目录 (./*.yaml)
4. 环境变量指定路径
```

---

## 配置文件

### 1. 应用配置 `~/.jobmatch/config.yaml`

```yaml
llm:
  provider: local
  local:
    base_url: http://localhost:11434
    model: qwen2.5:7b
    timeout: 180

storage:
  data_dir: ~/.jobmatch/data
  cache_dir: ~/.jobmatch/cache
  cache_enabled: true
  cache_ttl_days: 7
```

### 2. 匹配器配置 `matcher.yaml`

控制评分权重、阈值和模板：

```yaml
# 软性评分权重 (需合计为 1.0)
soft_score:
  weights:
    skill: 0.60        # 技能权重
    experience: 0.30   # 经验权重
    bonus: 0.10        # 加分项权重

  skill:
    base_point: 10              # 每项技能基础分
    inference_multiplier: 0.80  # 推断匹配折扣
    partial_multiplier: 0.50    # 部分匹配折扣

# 硬性门槛配置
hard_gate:
  borderline:
    delta_years: 1     # 边界判定阈值 (如要求5年，4年为边界)

  confidence:
    direct_match: 0.90   # 直接匹配置信度
    implied_match: 0.75  # 推断匹配置信度
    keyword_match: 0.70  # 关键词匹配置信度

# 差距分析配置
gap_analyzer:
  valuable_categories:
    - bigdata
    - ai_ml
    - cloud

  suggestion_templates:
    backend: "建议通过项目实践学习 {skill}，可以从官方文档和在线教程入手"
    database: "建议在本地搭建 {skill} 环境，通过实际操作学习"
    default: "建议系统学习 {skill}"
```

### 3. 技能推断规则 `skill_implications.yaml`

定义技能之间的推断关系：

```yaml
rules:
  - id: "java-spring-core"
    triggers:
      - Spring
      - Spring Boot
      - Spring MVC
    implies:
      - Java
      - Java Programming
      - Java编程
    confidence: 0.95
    evidence_template: "具备 {trigger} 经验，可推断掌握 {implied}"

  - id: "distributed-mq"
    triggers:
      - Kafka
      - RabbitMQ
      - RocketMQ
    implies:
      - Distributed Systems
      - Message Queue
      - 消息队列
    confidence: 0.85

# LLM 兜底配置
settings:
  llm_fallback:
    enabled: true
    min_confidence: 0.60
    cache_ttl_days: 7
```

---

## 输出报告示例

### Markdown 格式

```
╔══════════════════════════════════════════════════════════════╗
║                    匹配分析报告                               ║
╚══════════════════════════════════════════════════════════════╝

📊 总体评分: 78/100 - 良好匹配 (B级)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
一句话总结
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
较好匹配，技能覆盖度高，1项核心技能需补充

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
硬性门槛检查 (4/5 通过)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ 学历要求 (本科及以上) - 满足
  ✓ 工作年限 (5年+) - 满足 (8年)
  ✓ Java 技能 - 满足
  ✓ Java Programming - 通过 Spring Boot 可推断 (置信度 95%)
  ✗ Spring Cloud 经验 - 不满足

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
差距分析
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
缺失技能:
  • Spring Cloud [高影响] - 建议通过项目实践学习

核心优势:
  • 精通 Spring Boot - 在简历和面试中重点强调

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
行动建议
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
简历优化:
  1. [突出] 强调 Spring Boot 深度经验
  2. [补充] 添加分布式相关项目经验

学习计划:
  1. Spring Cloud 微服务组件 (预计1周)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
元数据
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  分析时间: 2025-12-13T14:11:09
  配置版本: 1.0.0
  规则版本: 1.0.0
```

---

## 其他命令

### 配置管理

```bash
jobmatch config              # 查看当前配置
jobmatch config show         # 显示所有配置 (含 matcher/rules 版本)
jobmatch config validate     # 验证配置正确性
```

**配置验证示例：**
```
Validating configuration...

Checking LLM connectivity...
  [?] Ollama at http://localhost:11434
      Run 'jobmatch analyze' to verify connectivity

Configuration validation passed!
  Errors: 0, Warnings: 0
```

### 历史记录

```bash
jobmatch history              # 列出所有记录
jobmatch history view <ID>    # 查看指定报告
jobmatch history delete <ID>  # 删除记录
jobmatch history clear        # 清空历史
```

### 缓存管理

```bash
jobmatch cache        # 查看缓存状态
jobmatch cache clean  # 清理过期缓存
jobmatch cache clear  # 清空所有缓存
```

### 用户反馈

```bash
jobmatch feedback              # 交互式提交反馈
jobmatch feedback -r 1         # 快速评分 (1=有帮助)
jobmatch feedback --stats      # 查看反馈统计
```

### 其他

```bash
jobmatch dict         # 查看技能词典统计
jobmatch --help       # 帮助信息
jobmatch --version    # 版本信息
```

---

## 自定义配置

### 添加自定义技能推断规则

创建 `~/.jobmatch/skill_implications.yaml`：

```yaml
version: "1.0.0-custom"

rules:
  # 添加自定义规则
  - id: "my-company-stack"
    triggers:
      - 公司内部框架名
    implies:
      - Java
      - Spring Boot
    confidence: 0.90
    evidence_template: "{trigger} 使用表明掌握 {implied}"
```

### 调整评分权重

创建 `~/.jobmatch/matcher.yaml`：

```yaml
version: "1.0.0-custom"

soft_score:
  weights:
    skill: 0.50        # 降低技能权重
    experience: 0.40   # 提高经验权重
    bonus: 0.10
```

---

## 错误处理

| 错误码 | 类型 | 常见原因 | 解决方案 |
|--------|------|----------|----------|
| 1001 | 简历为空 | 未提供简历内容 | 检查文件路径或输入 |
| 1002 | JD为空 | 未提供JD内容 | 检查文件路径或输入 |
| 3001 | LLM连接失败 | Ollama 未启动 | 运行 `ollama serve` |
| 3002 | LLM超时 | 模型响应慢 | 增加 timeout 或换小模型 |

---

## 开发

```bash
mvn compile              # 编译
mvn test                 # 运行测试
mvn package -DskipTests  # 打包
./install.sh             # 安装到本地
```

### 项目结构

```
job-match/
├── src/main/java/com/jobmatch/
│   ├── cli/           # CLI 命令层
│   ├── config/        # 配置管理 (新增)
│   │   ├── MatcherConfig.java
│   │   ├── MatcherConfigLoader.java
│   │   ├── SkillImplicationConfig.java
│   │   └── SkillImplicationLoader.java
│   ├── matcher/       # 匹配引擎 (新增)
│   │   ├── HardGateChecker.java
│   │   ├── SoftScoreCalculator.java
│   │   ├── GapAnalyzer.java
│   │   └── ActionGenerator.java
│   ├── skill/         # 技能服务 (新增)
│   │   └── SkillImplicationService.java
│   ├── parser/        # 解析器
│   ├── llm/           # LLM 集成
│   └── storage/       # 存储层
├── src/main/resources/
│   ├── application.yaml
│   ├── matcher.yaml              # 匹配器配置 (新增)
│   ├── skill_implications.yaml   # 技能推断规则 (新增)
│   └── skills_dictionary.yaml
└── examples/
    ├── sample_resume.txt
    └── sample_jd.txt
```

---

## License

MIT
